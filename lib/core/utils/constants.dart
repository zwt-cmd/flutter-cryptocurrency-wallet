/// 应用常量
class AppConstants {
  AppConstants._();

  /// API 配置
  static const apiBaseUrl = '';
  static const apiTimeout = Duration(seconds: 30);

  // APP配置
  static const String appName = 'My App';
  static const String version = '1.0.0';

  /// 分页配置
  static const defaultPageSize = 1;
}
